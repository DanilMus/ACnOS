{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "g++",
            "args": [
                "-g",
                "${workspaceFolder}/lib.cpp",
                "${workspaceFolder}/main_for_win.cpp",
                "-o",
                "${workspaceFolder}/main_for_win.exe"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
